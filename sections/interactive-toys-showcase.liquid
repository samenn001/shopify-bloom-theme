{{ 'section-interactive-toys.css' | asset_url | stylesheet_tag }}

<section class="interactive-toys-section">
  <div class="page-width">
    <!-- Section Header -->
    <div class="toys-header">
      <h2 class="toys-title">{{ section.settings.title | default: "✨ COMPLETE THEIR PARADISE WITH INTERACTIVE PLAY ✨" }}</h2>
      <p class="toys-subtitle">{{ section.settings.subtitle | default: "While trees give them a kingdom to rule, toys give them adventures to conquer" }}</p>
    </div>

    <!-- Three Videos Showcase -->
    <div class="toy-videos-grid">
      <div class="video-item">
        {% if section.settings.video1_url != blank %}
          <div class="toy-video-wrapper">
            <video 
              class="toy-video"
              controls
              playsinline
              muted
              autoplay
              loop
              preload="metadata"
            >
              <source src="{{ section.settings.video1_url }}" type="video/mp4">
            </video>
            <div class="sound-indicator-pill" style="z-index: 9999 !important; cursor: pointer;" onclick="var v=this.parentElement.querySelector('video');if(v){v.muted=!v.muted;this.classList.toggle('sound-on');this.innerHTML=v.muted?'🔇 Tap for Sound':'🔊 Sound On';if(!v.muted){v.play();}}">
              🔇 Tap for Sound
            </div>
          </div>
        {% endif %}
        <h4 class="video-title">{{ section.settings.video1_title | default: "Interactive Play" }}</h4>
      </div>

      <div class="video-item">
        {% if section.settings.video2_url != blank %}
          <div class="toy-video-wrapper">
            <video 
              class="toy-video"
              controls
              playsinline
              muted
              autoplay
              loop
              preload="metadata"
            >
              <source src="{{ section.settings.video2_url }}" type="video/mp4">
            </video>
            <div class="sound-indicator-pill" style="z-index: 9999 !important; cursor: pointer;" onclick="var v=this.parentElement.querySelector('video');if(v){v.muted=!v.muted;this.classList.toggle('sound-on');this.innerHTML=v.muted?'🔇 Tap for Sound':'🔊 Sound On';if(!v.muted){v.play();}}">
              🔇 Tap for Sound
            </div>
          </div>
        {% endif %}
        <h4 class="video-title">{{ section.settings.video2_title | default: "Active Engagement" }}</h4>
      </div>

      <div class="video-item">
        {% if section.settings.video3_url != blank %}
          <div class="toy-video-wrapper">
            <video 
              class="toy-video"
              controls
              playsinline
              muted
              autoplay
              loop
              preload="metadata"
            >
              <source src="{{ section.settings.video3_url }}" type="video/mp4">
            </video>
            <div class="sound-indicator-pill" style="z-index: 9999 !important; cursor: pointer;" onclick="var v=this.parentElement.querySelector('video');if(v){v.muted=!v.muted;this.classList.toggle('sound-on');this.innerHTML=v.muted?'🔇 Tap for Sound':'🔊 Sound On';if(!v.muted){v.play();}}">
              🔇 Tap for Sound
            </div>
          </div>
        {% endif %}
        <h4 class="video-title">{{ section.settings.video3_title | default: "Happy Cats" }}</h4>
      </div>
    </div>

    <!-- Why Combine Section -->
    <div class="why-combine-section">
      <h3 class="why-combine-title">{{ section.settings.why_combine_title | default: "WHY COMBINE TREES & TOYS?" }}</h3>
      
      <div class="daily-routine">
        <div class="routine-card">
          <div class="routine-icon">🌅</div>
          <h4>Morning</h4>
          <p>🎮 Toy Play</p>
          <span>Energy Release</span>
        </div>
        <div class="routine-card">
          <div class="routine-icon">☀️</div>
          <h4>Afternoon</h4>
          <p>🌳 Tree Nap</p>
          <span>Rest & Watch</span>
        </div>
        <div class="routine-card">
          <div class="routine-icon">🌙</div>
          <h4>Evening</h4>
          <p>🎯 Toy Hunt</p>
          <span>Playtime with You</span>
        </div>
      </div>
      
      <p class="happiness-stats">{{ section.settings.stats_text | default: "Studies show cats with both climbing and play options are 73% more active and happier" }}</p>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Interactive Toys Showcase",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Section Title",
      "default": "✨ COMPLETE THEIR PARADISE WITH INTERACTIVE PLAY ✨"
    },
    {
      "type": "textarea",
      "id": "subtitle",
      "label": "Section Subtitle",
      "default": "While trees give them a kingdom to rule, toys give them adventures to conquer"
    },
    {
      "type": "header",
      "content": "Video 1"
    },
    {
      "type": "text",
      "id": "video1_url",
      "label": "Video 1 URL"
    },
    {
      "type": "text",
      "id": "video1_title",
      "label": "Video 1 Title",
      "default": "Interactive Play"
    },
    {
      "type": "header",
      "content": "Video 2"
    },
    {
      "type": "text",
      "id": "video2_url",
      "label": "Video 2 URL"
    },
    {
      "type": "text",
      "id": "video2_title",
      "label": "Video 2 Title",
      "default": "Active Engagement"
    },
    {
      "type": "header",
      "content": "Video 3"
    },
    {
      "type": "text",
      "id": "video3_url",
      "label": "Video 3 URL"
    },
    {
      "type": "text",
      "id": "video3_title",
      "label": "Video 3 Title",
      "default": "Happy Cats"
    },
    {
      "type": "text",
      "id": "why_combine_title",
      "label": "Why Combine Title",
      "default": "WHY COMBINE TREES & TOYS?"
    },
    {
      "type": "textarea",
      "id": "stats_text",
      "label": "Statistics Text",
      "default": "Studies show cats with both climbing and play options are 73% more active and happier"
    }
  ],
  "presets": [
    {
      "name": "Interactive Toys Showcase"
    }
  ]
}
{% endschema %}